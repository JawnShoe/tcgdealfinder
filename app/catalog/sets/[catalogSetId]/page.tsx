/**
 * Legacy route placeholder.
 * The catalog set detail UI was cut from v1, but Next.js still generates type stubs
 * for this segment unless a module exists. Keeping this no-op page prevents
 * build-time type errors without exposing any UI.
 */
export default function CatalogSetPagePlaceholder() {
  return null;
}

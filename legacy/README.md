# Legacy Quarantine (reference only)

This directory holds pre-rebuild implementation files that are not used by active surfaces.

Rules:

- Reference-only. Do not import from /legacy/\*\*.
- Do not edit files here except to move additional unused legacy files.
- All new work must live in the rebuild lane or approved active surfaces.

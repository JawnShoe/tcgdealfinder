-- Deterministic seed data for rebuild E2E smoke tests.

-- Create a test card for cardId->listingId mapping tests (Stage 1 decommission: /cards/[cardId])
-- Uses id=99999 to avoid conflicts with production data
INSERT INTO cards (
  id,
  name,
  set_name,
  card_number,
  condition_bucket
) VALUES (
  99999,
  'E2E Test Card',
  'E2E Test Set',
  '001',
  'NM'
)
ON CONFLICT (id) DO UPDATE SET
  name = EXCLUDED.name,
  set_name = EXCLUDED.set_name,
  card_number = EXCLUDED.card_number,
  condition_bucket = EXCLUDED.condition_bucket;

INSERT INTO listings (
  listing_id,
  title,
  url,
  source,
  price_cad,
  total_price_cad,
  total_native,
  currency,
  price_native,
  discount_percent,
  condition_raw,
  shipping_known,
  seller,
  seller_username,
  seller_feedback_count,
  seller_positive_percent,
  market,
  ends_at,
  deal_confidence_weight,
  integrity_status,
  integrity_reason,
  snapshot_at,
  ingested_at,
  updated_at,
  created_at,
  card_id
) VALUES (
  'rebuild-e2e-1',
  'Rebuild E2E Listing',
  'https://example.com/rebuild-e2e-1',
  'EBAY',
  100.00,
  120.00,
  120.00,
  'USD',
  100.00,
  12.34,
  'NM',
  true,
  'E2E Seller',
  'e2e_seller',
  250,
  99.8,
  'EBAY_US',
  TIMESTAMPTZ '2026-01-25 18:00:00+00',
  0.8,
  'OK',
  NULL,
  '2026-01-01T00:00:00Z',
  '2026-01-01T00:00:00Z',
  '2026-01-01T00:00:00Z',
  '2026-01-01T00:00:00Z',
  99999
)
ON CONFLICT (listing_id) DO UPDATE SET
  title = EXCLUDED.title,
  url = EXCLUDED.url,
  source = EXCLUDED.source,
  price_cad = EXCLUDED.price_cad,
  total_price_cad = EXCLUDED.total_price_cad,
  total_native = EXCLUDED.total_native,
  currency = EXCLUDED.currency,
  price_native = EXCLUDED.price_native,
  discount_percent = EXCLUDED.discount_percent,
  condition_raw = EXCLUDED.condition_raw,
  shipping_known = EXCLUDED.shipping_known,
  seller = EXCLUDED.seller,
  seller_username = EXCLUDED.seller_username,
  seller_feedback_count = EXCLUDED.seller_feedback_count,
  seller_positive_percent = EXCLUDED.seller_positive_percent,
  market = EXCLUDED.market,
  ends_at = EXCLUDED.ends_at,
  deal_confidence_weight = EXCLUDED.deal_confidence_weight,
  integrity_status = EXCLUDED.integrity_status,
  integrity_reason = EXCLUDED.integrity_reason,
  snapshot_at = EXCLUDED.snapshot_at,
  ingested_at = EXCLUDED.ingested_at,
  updated_at = EXCLUDED.updated_at,
  created_at = EXCLUDED.created_at,
  card_id = EXCLUDED.card_id;
